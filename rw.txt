=== resend otp for forgot password after the previous expires ===
=== custom OTP timouts ===
=== Delete OTP when timedOut so that user can request new, Also update the message of wait for it to expire ===
=== ?q= in /get
✓ Pagination
✓ Sorting
✓ Filtering
✓ Search
✓ Field selection
✓ Invalid query params
✓ Large dataset
===

=== COLLECTION ===
Skips:
Route:
  1. middlewareArray
  2. emailsArray

MongooseSchema:
  1. provider

ReqType:
  1. realtime
-----------------------------------------------------------------------------

handlers/
├── auth/          // login, signup, logout, refresh, 2FA...
├── crud/          // create, read, update, delete, bulk...
├── middleware/    // auth, rateLimit, cors, validation...
├── upload/        // file/image uploads
├── webhook/       // webhook handlers
├── realtime/      // socket/sse
├── admin/         // dashboard/admin-only handlers
├── health/        // health, ping, metrics
├── system/        // internal framework handlers
├── utils/
├── account/
│   ├── me/
│   │   ├── get.ts
│   │   ├── update.ts
│   │   └── delete.ts
│   │
│   ├── password/
│   │   ├── change.ts
│   │   └── ...
│   │
│   ├── email/
│   │   ├── change.ts
│   │   └── ...
│   │
│   ├── sessions/
│   │   ├── list.ts
│   │   ├── revoke.ts
│   │   └── revokeAll.ts
│   │
│   ├── devices/
│   │   ├── list.ts
│   │   └── revoke.ts
│   │
│   └── preferences/
│       ├── get.ts
│       └── update.ts

=== GIT COMMANDS ===
git switch main
git pull origin main
git push
git switch feature
git pull origin main
git push
git switch main
git diff feature

admin/

client:accessToken -> auth.middleware.ts -> im.admin.ts

auth.middleware.ts:
  if verifyAccessUser -> req.user = _id: user._id, role: user.role

im.admin.ts:
  1. If not req.user.role?  Get User model. Find the user with req.user._id
  2. if !internalRoles.include(user.role)
        throw new AppError({})
  3. return success: true