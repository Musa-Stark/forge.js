=== resend otp for forgot password after the previous expires ===
=== custom OTP timouts ===
=== ?q= in /get
✓ Pagination
✓ Sorting
✓ Filtering
✓ Search
✓ Field selection
✓ Invalid query params
✓ Large dataset
===

=== emails: [
  {
    event: "productCreated",
    to: "{{owner.email}}",
  },
]; ===

=== cloudinary custom folder name ===
=== Unexpected file field: backgroundImage. File field must match collection's fileArray -> fieldName | When formdata/... is sent instead of app/json and it is not needed. ===

=== COLLECTION ===
Skips:
Route:
  1. middlewareArray
  2. emailsArray

MongooseSchema:
  1. provider

ReqType:
  1. realtime
-----------------------------------------------------------------------------

handlers/
├── auth/          // login, signup, logout, refresh, 2FA...
├── crud/          // create, read, update, delete, bulk...
├── middleware/    // auth, rateLimit, cors, validation...
├── upload/        // file/image uploads
├── webhook/       // webhook handlers
├── realtime/      // socket/sse
├── admin/         // dashboard/admin-only handlers
├── health/        // health, ping, metrics
├── system/        // internal framework handlers
└── utils/

automating auth + manual option

console:
auth -> http://localhost:10000/api/v1/auth/[login, signup, forgot-password, verify-otp, reset-password]

StarkForge:
import { fields } from "forge.js";

const authConfig = {
  mode: "builtIn",
  user: {
    modelName: "User",
    schema: {
      name: fields.requiredString,
      email: fields.email,
      password: fields.password,
      role: fields.role,
      isVerified: fields.booleanFalse,
      // any extra fields they want
    },
  },
  authMode: "credentials", // or "otp" | "magic-link"
};

=== Forge using it unified fields ===
// Inside built-in auth setup
const mongooseSchema = buildMongooseSchema(authConfig.user.schema);
const User = mongoose.model(authConfig.user.modelName, mongooseSchema);

// For signup validation
const signupSchema = buildZodObject(authConfig.user.schema, [
  "name",
  "email",
  "password",
]);

// For login
const loginSchema = buildZodObject(authConfig.user.schema, ["email", "password"]);

// For forgot password
const forgotSchema = buildZodObject(authConfig.user.schema, ["email"]);

 password: {
    mongoose: {
      type: [Function: String],
      select: false
    },
    zod: ZodString {
      toJSONSchema: [Function (anonymous)],
      def: [Object],
    }
  }
}
[ 'name', 'email', 'password', 'otp' ]
